- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.tmux.conf:
    ~/.vimrc:
    ~/.zshrc:
    ~/.vim:
    ~/.gitconfig:


- create:
    - ~/downloads
    - ~/.vim/undo-history

- shell:
    - [git submodule update --init --recursive, Installing submodules]
